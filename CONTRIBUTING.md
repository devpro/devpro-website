# Contibuting

Website files (HTML) is built with [Eleventy (11ty)](https://www.11ty.dev/).

## Local run

Install JavaScript dependencies:

```bash
npm install
```

Run locally the website:

```bash
npm start
```
